var ra ="";
var link ="";
  var url="https://console.firebase.google.com/project/manual-polling-3ed91/database/manual-polling-3ed91/data/Test1/";
        
  var chars = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXTZabcdefghiklmnopqrstuvwxyz";
  var string_length = 8;
  var randomstring = '';
  for (var i=0; i<string_length; i++) {
    var rnum = Math.floor(Math.random() * chars.length);
    randomstring += chars.substring(rnum,rnum+1);
  }
  var int =randomstring;
  var ra = randomstring;
var res = link.concat(url, int);
window.load(res)

  function submitOrder() {
              var link ="";
  var url="https://console.firebase.google.com/project/manual-polling-3ed91/database/manual-polling-3ed91/data/Test1/";
        
  var chars = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXTZabcdefghiklmnopqrstuvwxyz";
  var string_length = 8;
  var randomstring = '';
  for (var i=0; i<string_length; i++) {
    var rnum = Math.floor(Math.random() * chars.length);
    randomstring += chars.substring(rnum,rnum+1);
  }
  var int =randomstring;
  var ra = randomstring;
var res = link.concat(url, int);
window.load(res)
//document.write(res);

}